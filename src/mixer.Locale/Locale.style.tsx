import styled from 'styled-components';

export const Wrapper = styled.div`
    flex: 1;
    margin: 20px;
`;

export const Title = styled.div`
    font-size: 18px;
    flex: 1;

    &.selected {
        color: gold;
    }
`;
