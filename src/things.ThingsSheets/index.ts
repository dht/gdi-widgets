import { ThingsSheetsContainer } from './ThingsSheets.container';

export const widgets = {
    'com.usegdi.widgets.things.Sheets': {
        id: 'com.usegdi.widgets.things.Sheets',
        component: ThingsSheetsContainer,
    },
};
