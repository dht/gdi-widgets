import React from 'react';
import { ScheduleTop } from './ScheduleTop';
import { soundboard as store } from '@gdi/selectors';

export const ScheduleTopContainer = () => {
    return <ScheduleTop />;
};

export default ScheduleTopContainer;
