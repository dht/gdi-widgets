import { MixerContainer } from './Mixer.container';

export const widgets = {
    'com.usegdi.widgets.mixer.Mixer': {
        id: 'com.usegdi.widgets.mixer.Mixer',
        component: MixerContainer,
    },
};
